# ── DeepRT requirements ────────────────────────────────────────────────────────
#
# PyTorch (install separately with the right CUDA wheel before running this):
#   pip install torch torchvision torchaudio \
#       --index-url https://download.pytorch.org/whl/cu128
#
# PyG backends (pre-built wheels; adjust the torch/CUDA tag to match your env):
#   pip install torch-scatter torch-sparse torch-cluster \
#       -f https://data.pyg.org/whl/torch-2.7.1+cu128.html

# ── Graph neural networks ──────────────────────────────────────────────────────
--find-links https://data.pyg.org/whl/torch-2.7.1+cu128.html
torch-geometric>=2.0
torch-scatter
torch-sparse
torch-cluster

# ── HuggingFace / datasets ─────────────────────────────────────────────────────
datasets>=2.0
huggingface-hub>=0.20
tokenizers>=0.15

# ── Protein language model ─────────────────────────────────────────────────────
esm>=3.0,<3.2.2        # 3.2.2+ requires Python >=3.12

# ── Chemistry ─────────────────────────────────────────────────────────────────
rdkit>=2023.0

# ── Scientific computing ───────────────────────────────────────────────────────
numpy>=1.24
scipy>=1.10
scikit-learn>=1.2
pandas>=1.5

# ── Training utilities ────────────────────────────────────────────────────────
tqdm>=4.0
einops>=0.6
lightning>=2.0
pytorch-lightning>=2.0
tensorboard>=2.10
matplotlib>=3.5
